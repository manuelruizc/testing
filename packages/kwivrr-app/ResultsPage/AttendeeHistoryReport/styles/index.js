const styles = () => {
    return {
        container: {
            flex: 1,
            justifyContent: 'flex-start',
            alignItems: 'flex-start',
        },
    };
};

export default styles;
