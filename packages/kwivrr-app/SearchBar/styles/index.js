const styles = ({ screenWidth, screenHeight }) => {
    return {
        container: {
            width: '100%',
            height: 200,
            position: 'absolute',
            top: 0,
            left: 0,
            marginTop: 92,
            backgroundColor: 'tomato',
        },
        textButton: {
            // font
        },
        backdropTouchable: {
            width: '100%',
            height: '100%',
        },
    };
};

export default styles;
