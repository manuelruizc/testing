const styles = ({ dwPercentage }) => {
    return {
        scrollView: {
            flex: 1,
            width: '100%',
            zIndex: -1,
        },
        contentContainer: {
            alignItems: 'center',
            width: '100%',
        },
    };
};

export default styles;
